import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  background: {
    black: "#000000",
  },
  button: {
    blue: "#0039A6",
  },
  text: {
    white: "#FFFFFF",
  },
});

export default theme;
